spring:
  datasource:
    url: jdbc:mysql://192.168.219.108:3306/fit_challenge_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: user
    password: kho13241003567
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect # 명시적으로 Dialect 지정
    show-sql: true
  redis:
    host: 192.168.219.108
    port: 6379

logging:
  level:
    com.fit.auth: DEBUG

